# Функционал Редактирования Таблиц - Документация

## 📝 Обзор

Добавлен универсальный функционал редактирования таблиц (как в Master Engine List) для всех модулей:
- ✅ Installation
- ✅ Shipment
- ✅ Remove
- ✅ Repair
- ✅ Utilization (ATLB)

## 🎯 Основные Функции

### 1. Кнопки Управления

Для каждой таблицы добавлены кнопки:
- **Edit** (желтая) - включить режим редактирования
- **Save Changes** (зеленая) - сохранить изменения
- **Cancel** (серая) - отменить изменения
- **+ (круглая зеленая)** - добавить новую строку

### 2. Режим Редактирования

При нажатии **Edit**:
- Все строки подсвечиваются **желтым фоном**
- Каждая ячейка превращается в поле ввода
- Поля имеют **желтую рамку** с эффектом свечения
- При фокусе свечение становится **зеленым**

### 3. Добавление Строк

При нажатии кнопки **+**:
- Добавляется новая пустая строка в конец таблицы
- Строка подсвечивается **зеленым** для визуального выделения
- Все поля готовы для ввода данных

### 4. Типы Полей

Система автоматически определяет тип поля:
- **Date** - поля с датами
- **Number** - числовые поля (TT, TC, Position, Cycles)
- **Text** - текстовые поля (все остальные)

## 🔧 Технические Детали

### Конфигурация Таблиц

Каждая таблица имеет конфигурацию в `tableConfigs`:

```javascript
{
    bodyId: 'install-history-body',           // ID tbody элемента
    columns: ['date', 'original_sn', ...],     // Список колонок
    columnTypes: {                             // Типы полей для конкретных колонок
        date: 'date',
        position: 'number',
        tt: 'number'
    },
    reloadFunction: loadInstallHistory,        // Функция перезагрузки
    hasDeleteCol: true                         // Есть ли колонка удаления
}
```

### Универсальные Функции

#### `toggleEditModeTable(tableName)`
Включает режим редактирования:
- Скрывает кнопку Edit
- Показывает кнопки Save, Cancel, Add
- Превращает ячейки в input поля

#### `addEmptyRowTable(tableName)`
Добавляет пустую строку с зеленым фоном

#### `saveEditModeTable(tableName)`
Собирает данные из всех полей и сохраняет (в будущем - на сервер)

#### `cancelEditModeTable(tableName)`
Отменяет изменения и перезагружает таблицу с сервера

## 🎨 CSS Стили

### `.editing-row`
```css
background-color: #fff3cd !important; /* Желтый фон */
```

### `.editing-input`
```css
border: 2px solid #ffc107;           /* Желтая рамка */
box-shadow: 0 0 8px rgba(255, 193, 7, 0.6);  /* Свечение */
```

### `.editing-input:focus`
```css
box-shadow: 0 0 12px rgba(44, 211, 3, 0.9);  /* Зеленое свечение при фокусе */
```

### `.btn-circle`
```css
width: 38px;
height: 38px;
border-radius: 50%;                   /* Круглая кнопка */
```

### `.table-success`
```css
/* Применяется к новым строкам для подсветки зеленым */
```

## 📋 Примеры Использования

### Редактирование Существующей Записи

1. Откройте модуль (например, Installation)
2. Нажмите **Edit** (желтая кнопка)
3. Все строки станут редактируемыми с желтым фоном
4. Измените нужные значения
5. Нажмите **Save Changes** для сохранения
6. Или **Cancel** для отмены

### Добавление Новой Записи

1. Нажмите **Edit**
2. Нажмите круглую кнопку **+**
3. Появится новая зеленая строка внизу
4. Заполните поля
5. Нажмите **Save Changes**

### Отмена Изменений

1. В любой момент режима редактирования
2. Нажмите **Cancel**
3. Таблица перезагрузится из базы данных
4. Все несохраненные изменения будут потеряны

## ⚠️ Важные Замечания

1. **Сохранение на сервер** - В текущей версии функция `saveEditModeTable()` выводит данные в консоль. Для полноценного сохранения необходимо:
   - Создать API endpoint для bulk update
   - Реализовать отправку данных на сервер
   - Добавить обработку ошибок

2. **Utilization Table** - Не имеет delete-col, поэтому используется `hasDeleteCol: false`

3. **Валидация данных** - Необходимо добавить проверку корректности введенных данных перед сохранением

4. **Конфликты при одновременном редактировании** - Если несколько пользователей редактируют одновременно, возможны конфликты данных

## 🚀 Будущие Улучшения

1. **Автосохранение** - Сохранять изменения автоматически при потере фокуса
2. **Отмена по Ctrl+Z** - История изменений
3. **Валидация в реальном времени** - Подсветка ошибок красным
4. **Drag & Drop** - Перетаскивание строк для изменения порядка
5. **Экспорт в Excel** - Возможность экспорта отредактированных данных

## 📊 Статус Реализации

| Модуль | Edit Mode | Add Row | Save | Cancel | Статус |
|--------|-----------|---------|------|--------|---------|
| Installation | ✅ | ✅ | ✅ | ✅ | 🟢 Готово |
| Shipment | ✅ | ✅ | ✅ | ✅ | 🟢 Готово |
| Remove | ✅ | ✅ | ✅ | ✅ | 🟢 Готово |
| Repair | ✅ | ✅ | ✅ | ✅ | 🟢 Готово |
| Utilization | ✅ | ✅ | ✅ | ✅ | 🟢 Готово |

**Легенда:**
- 🟢 Полностью реализовано
- 🟡 Частично реализовано
- ⚠️ Требует доработки
- ✅ Работает

---

**Дата создания:** 30 ноября 2025  
**Версия:** 1.0  
**Автор:** GitHub Copilot
