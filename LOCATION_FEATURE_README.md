# 📋 ИНСТРУКЦИЯ ПО ВНЕДРЕНИЮ ФУНКЦИИ "CURRENT LOCATION"

## ✅ ЧТО БЫЛО РЕАЛИЗОВАНО

Добавлена функциональность выбора локации (города) при добавлении нового двигателя.
Теперь при добавлении двигателя система:
1. **Сохраняет его локацию** в базу данных
2. **Отображает счетчик** двигателей на каждой кнопке локации в Dashboard
3. **Показывает таблицу** со всеми двигателями при клике на кнопку локации

---

## 🔧 ИЗМЕНЕНИЯ В КОДЕ

### 1. **Frontend (index.html)**

#### ✅ Добавлено поле "Current Location" в форму
- Местоположение: Секция "Добавить двигатель" → вкладка "Ввод основных данных"
- Тип: Выпадающий список (dropdown) с локациями
- Обязательное поле (*)

#### ✅ Обновлена функция `submitMainEngine()`
- Теперь проверяет заполнение локации
- Отправляет данные на сервер через API POST `/api/engines`
- Обновляет Dashboard после успешного добавления

#### ✅ Добавлена функция `loadLocationsForForm()`
- Загружает список локаций из API `/api/locations`
- Заполняет выпадающий список при открытии формы

#### ✅ Обновлена функция `openLocationModal()`
- Показывает все двигатели в выбранной локации
- Добавлена колонка "Model" в таблицу

### 2. **Backend (main.py)**

#### ✅ Добавлен API endpoint `POST /api/engines`
- Создает новый двигатель с привязкой к локации
- Проверяет уникальность серийного номера
- Создает запись в истории (Action Log)

#### ✅ Обновлен endpoint `GET /api/locations`
- Возвращает количество двигателей (`engine_count`) для каждой локации

#### ✅ Обновлен endpoint `GET /api/engines`
- Теперь возвращает поля: `model`, `gss_sn`, `photo_url`

### 3. **Models (models.py)**

#### ✅ Добавлены новые поля в таблицу `Engine`:
- `model` - Модель двигателя (CF6-80, CFM56 и т.д.)
- `photo_url` - Ссылка на фото двигателя
- `n1_takeoff`, `n1_cruise`, `n2_takeoff`, `n2_cruise` - Параметры для мониторинга

---

## 🚀 КАК ЗАПУСТИТЬ

### ШАГ 1: Миграция базы данных
Запустите скрипт миграции **ОДИН РАЗ** для обновления структуры БД:

```powershell
python migrate_db.py
```

Должны появиться сообщения:
```
✓ Добавлено поле 'model'
✓ Добавлено поле 'photo_url'
✓ Добавлено поле 'n1_takeoff'
...
✅ Миграция завершена успешно!
```

### ШАГ 2: Запуск сервера
```powershell
python -m uvicorn backend.main:app --reload
```

Или двойной клик по файлу `start_server.bat`

### ШАГ 3: Откройте браузер
```
http://localhost:8000
```

---

## 📖 КАК ПОЛЬЗОВАТЬСЯ

### 1. Добавление двигателя с локацией

1. Откройте меню **"Master Engine List"** (слева в боковом меню)
2. Перейдите на вкладку **"Добавить двигатель"**
3. Заполните поля:
   - **Date** - дата добавления
   - **Model** - модель (например CF6-80C2)
   - **Orig. ESN*** - серийный номер (ОБЯЗАТЕЛЬНО)
   - **GSS ID** - ваш внутренний ID
   - **Status** - выберите статус (SV/US/AS)
  - **Current Location*** - **ВЫБЕРИТЕ ЛОКАЦИЮ** (SHJ, FRU, DXB, MIAMI, Rome (Italy))
   - **TT** и **TC** - наработка (по умолчанию 0)
   - **Photo (Link)** - ссылка на фото (необязательно)
   - **Remarks** - примечания

4. Нажмите **"Save Record"**

### 2. Просмотр двигателей по локациям

На главном Dashboard (кнопка **"Dashboard"** в боковом меню):

- Вы увидите кнопки локаций: **SHJ (Sharjah)**, **FRU (Bishkek)**, **DXB (Dubai)**, **MIAMI (Miami)**, **Rome (Italy)**
- На каждой кнопке отображается **количество двигателей** в этой локации
- **Кликните** на любую кнопку → откроется **таблица** со всеми двигателями

---

## 🎯 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Добавление двигателя в Шарджу
```
Model: CF6-80C2
Orig. ESN: 123456
Status: SV (Serviceable)
Current Location: SHJ - Sharjah
TT: 0
TC: 0
```

После сохранения:
- Двигатель появится в базе
- На Dashboard кнопка "SHJ (Sharjah)" покажет счетчик: **1**
- При клике на кнопку откроется таблица с этим двигателем

### Пример 2: Просмотр всех двигателей в Бишкеке
1. Откройте Dashboard
2. Найдите кнопку **"FRU (Bishkek)"**
3. Кликните → увидите таблицу со всеми двигателями в FRU

---

## 🔍 ПРОВЕРКА РАБОТЫ

### 1. Проверка API
Откройте в браузере:
```
http://localhost:8000/api/locations
```

Должны увидеть:
```json
[
  {
    "id": 1,
    "name": "SHJ",
    "city": "Sharjah",
    "engine_count": 0
  },
  ...
]
```

### 2. Проверка Dashboard
- Откройте главную страницу
- Должны быть видны 4 кнопки локаций с цифрой "0"
- После добавления двигателя цифра должна увеличиться

---

## ❗ ВАЖНО

1. **Не изменяйте старый код** - все изменения ДОБАВЛЕНЫ, ничего не удалено
2. **Миграцию запускать ОДИН РАЗ** - повторный запуск безопасен, но не обязателен
3. **Локации создаются автоматически** при первом запуске сервера
4. **Русские буквы в ESN запрещены** - валидация на стороне клиента

---

## 🐛 УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема: "Location не обязательное поле"
**Решение:** Обновите страницу (Ctrl+F5), очистите кеш браузера

### Проблема: "Ошибка сервера при сохранении"
**Решение:** 
1. Проверьте, запущена ли миграция: `python migrate_db.py`
2. Перезапустите сервер

### Проблема: "Счетчик локаций не обновляется"
**Решение:** Нажмите F5 для обновления Dashboard

---

## 📞 СЛЕДУЮЩИЕ ШАГИ

Если нужно добавить еще функции:
1. Фильтрация двигателей по статусу в локации
2. Перемещение двигателя между локациями
3. История перемещений
4. Экспорт данных в Excel

---

✅ **ФУНКЦИЯ ПОЛНОСТЬЮ ГОТОВА К ИСПОЛЬЗОВАНИЮ!**
