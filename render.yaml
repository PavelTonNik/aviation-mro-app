services:
  - type: web
    name: aviation-mro
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: aviation_db
          property: connectionString

databases:
  - name: aviation_db
    plan: free